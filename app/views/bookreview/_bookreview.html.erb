    <%= link_to 'bookreview.image', "/bookreview/#{bookreview.id}", method: :get %>
    <%= link_to 'bookreview.name', "/bookreview/#{bookreview.id}", method: :get %>
    <%= bookreview.image %>
    <%= bookreview.name %>
    <%= bookreview.description %>
    <%= bookreview.price %>
    <%= bookreview.publisher %>