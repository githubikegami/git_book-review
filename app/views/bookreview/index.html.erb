
  <% @bookreviews.each do |bookreviews| %>
    <%= link_to 'bookreviews.image', "/bookreview/#{bookreview.id}", method: :get %>
    <%= link_to 'bookreviews.name', "/bookreview/#{bookreview.id}", method: :get %>
    <%= bookreviews.description %>
    <%= bookreviews.price %>
    <%= bookreviews.publisher %>
  <% end %>
  <%= paginate(@bookreviews) %>
